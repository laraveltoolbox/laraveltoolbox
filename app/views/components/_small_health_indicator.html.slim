.project-health-tag class="level-#{project.health.overall_level}"
  a href=project_href(project) title=project.health.status.map(&:label).join(", ")
    .tags.has-addons
      span.tag
        span.icon: i.fa class="fa-heartbeat"
