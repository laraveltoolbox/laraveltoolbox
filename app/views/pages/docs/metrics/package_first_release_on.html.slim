= render "metric_date_documentation", table: :packages, column: :first_release_on do
  / There are some awkward incorrect past / future dates for some packages in the registry
  - scope = Project.for_display.where("packages.first_release_on >= ? AND packages.first_release_on <= ?", Date.new(2004), Time.current)
  .columns
    .column
      = project_ranking "Oldest packages", table: :packages, column: :first_release_on, direction: "ASC", scope: scope
    .column
      = project_ranking "Newest packages", table: :packages, column: :first_release_on, scope: scope

