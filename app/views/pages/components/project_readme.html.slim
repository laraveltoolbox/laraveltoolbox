.hero
  section.section: .container
    p.heading= link_to "Laravel Toolbox UI Components Styleguide", "/pages/components"
    h2= current_page.split("/").last.humanize

- readme = Project.find("nunomaduro/collision").github_repo_readme

= component_example "Project README" do
  = project_readme readme

= component_example "Project README is not present" do
  = project_readme nil
